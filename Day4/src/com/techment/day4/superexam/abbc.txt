cd "path name "
git config --global user.name " "
git config --global user.email 
touch .gitignore
git init
git add .
git status
git commit -m "msg "
git status

if want to add a new file,then,
git add abbc.txt

git remote add origin githubLink

git push origin master


